/// <reference types="react" />
import * as React from 'react';
import { FormikProps, FormikState } from 'formik';
import * as PropTypes from 'prop-types';
export interface EffectProps<Values = {}> {
    onChange(currentState: FormikState<Values>, nextState: FormikState<Values>): void;
}
export declare class Effect<Values = {}> extends React.Component<EffectProps<Values>, {}> {
    static contextTypes: {
        formik: PropTypes.Requireable<any>;
    };
    componentWillReceiveProps(_nextProps: EffectProps<Values>, nextContext: {
        formik: FormikProps<Values>;
    }): void;
    render(): null;
}
